This project bundles third-party libraries (via npm) in the built extension output (`dist/`).

- DOMPurify (via npm `dompurify`)
- water.css (via npm `water.css`)

Refer to each package’s license in `node_modules/` when distributing the extension.
